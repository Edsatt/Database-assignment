package edu.uob;

public enum TokenType {
    COMMAND,
    BOOLEAN,
    COMPARATOR,
    PLAIN_TEXT,
    STRING_LITERAL,
    LETTER,
    INTEGER,
    FLOAT,
    SYMBOL,
    SPACE,
    WILD,
    INVALID;
}
